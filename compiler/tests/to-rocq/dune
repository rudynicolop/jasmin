(library
  (name common_to_rocq)
  (wrapped false)
  (libraries jasmin.jasmin)
  (modules common))

(tests
  (libraries jasmin.jasmin common_to_rocq threads)
  (deps
    (glob_files ../success/x86-64/*))
  (modules accept)
  (names accept))
