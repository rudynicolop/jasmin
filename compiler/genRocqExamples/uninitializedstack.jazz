/* Simple program, which returns the result of an unitialized
   but declared stack variable. */

export
fn main () -> reg u32 {
   stack u32 x;
   // Emits a warning.
   reg u32 y = x;
   // Returns 0, is this random?
   return y;
}
